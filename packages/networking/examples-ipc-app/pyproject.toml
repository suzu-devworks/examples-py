[project]
authors = [
  { name = "A.suzuki", email = "suzu.devworks@gmail.com" },
]
dependencies = [
  "posix-ipc>=1.3.2",
  "sysv-ipc>=1.2.0",
]
description = "Add your description here"
name = "examples-ipc-app"
readme = "README.md"
requires-python = ">=3.13"
version = "0.1.0"

[project.scripts]
examples-ipc-app = "examples_ipc_app:main"

[build-system]
build-backend = "uv_build"
requires = ["uv_build>=0.12.0,<0.13.0"]
